# 会议纪要 · 2026-09-04 · Z-04 MCP 与 CLI 工具台账定案

## 一、批准事实
用户 2026-09-04 批准「候选 T-04 → 总则 Z-04」：MCP 服务器与 CLI 工具同样采用登记制。

**总则 Z-04 · MCP 服务与 CLI 工具台账（结论板原文，字面保留）**
本机 Claude Code 生态的 MCP 服务器与 CLI 工具，同样采用登记制：实体各归标准生效位置（MCP 注册于 Claude Code 配置——项目 .mcp.json / 用户级 ~/.claude.json 等；CLI 位于 PATH/已知路径），不搬文件、不做目录联接。D:\mcp\ 与 D:\cli\ 各设 index.html 总台账登记（D 盘根下平级，与 D:\model、D:\skill 相互独立）。编号 MCP-001… 与 CLI-001… 只增不复用。登记含：名称、能力、来源、添加日期、位置/注册 scope、启动命令、状态；含凭据/远程项只记所需环境变量名、明文不入台账；npx/uvx 或工具自动托管者只登记不搬家（物理收拢缓存另行批准）。新增、删除、停用任一 MCP/CLI 后须更新台账；跨 skill/MCP/CLI 的同源工具做编号互链，不重复登记。

## 二、P-8 实战（治理体系第 4 次实战 · 小希·书记长 领衔）
书记长编排 **3 名书记子 Agent 并行**、各管一页、互不覆盖：

- **书记·草案 → 草案.html v0.11**：候选 0 / 空收件箱。移除整块 T-04 候选卡；换绿色定案横幅「候选 T-04（MCP 服务器与 CLI 工具台账）已定案 · 已纳入 总则 Z-04 / 执行结构 E-04」；放回「暂无候选在审」占位卡并更新四条定案轨迹（T-04→Z-04、T-03→Z-03、T-02→Z-02、T-01→Z-01）；meta 与变更记录（v0.11）同步。
- **书记·总则 → 总则.html v0.7 + CLAUDE.md 镜像**：新增 Z-04 结论卡（id=z-04，分类「文件与存储规范」，视觉同 z-02/z-03 蓝色系），条文用 Z-04 结论板原文逐字拷贝；hero 计数 3→4、筛选胶囊「全部 4 / 文件与存储规范 3」、变更记录 v0.6→v0.7 追加行；**同一轮**把 Z-04 镜像**追加**至 C:\Users\pc\.claude\CLAUDE.md 末尾（Z-01/Z-02/Z-03 原块未损）。
- **书记·执行 → 执行结构.html v0.8**：新增正式对开条目 E-04（id=E-04），**三小节齐全**——①落地说明（MCP/CLI 为什么只能登记不能搬、注册 config/scope 差异、两本平行台账字段与编号只增不复用、凭据只记环境变量名明文不入台账、claude mcp remove/list 官方通道、npx/uvx 与工具托管只登记不搬家、跨 skill/MCP/CLI 同源互链不重复登记）；②通俗解释（水电管网接口/物业登记本比喻：能记不能搬、只记电表不抄钥匙）；③名词解释（17 词条）。索引表增行 Z-04↔E-04，返回锚点 总则.html#z-04。

**编排核验（书记长）**：三书记完成通知均已收到；书记长本人 Read 复核四页 + CLAUDE.md + 两本新台账，确认 z-04 结论卡含原文、总则计数=4、E-04 三小节齐全、锚点 z-04↔E-04 双向链通。

## 三、台账建档与巡检登记结果（据实）
书记长本人于 D 盘根下平级新建 `D:\mcp\` 与 `D:\cli\`（均直接位于 D:\，与 D:\model、D:\skill 平级，无套父夹），各写 index.html 台账（样式仿 D:\skill\index.html）。

- **MCP 台账（D:\mcp\index.html）**：巡检 `claude mcp list` 返回 **No MCP servers configured**（用户 settings.json 亦无 mcpServers 段）→ 如实记 **「暂无在编 MCP」**，登记 0 条，编号 MCP-001… 预留待补。未虚构任何条目。
- **CLI 台账（D:\cli\index.html）**：Get-Command / npm 目录实测定位 2 条并登记：
  - **CLI-001 claude** —— Claude Code 本体 CLI（含 claude mcp add/list/remove），npm 全局 @anthropic-ai/claude-code，路径 C:\Users\pc\AppData\Roaming\npm\claude（.ps1/.cmd），claude --version = 2.1.259，状态 在编。
  - **CLI-002 tts_open_toolkit** —— TikTok Shop OpenAPI/Open Platform CLI，npm 全局 @tts-open-toolkit/cli v0.1.7，路径 C:\Users\pc\AppData\Roaming\npm\tts_open_toolkit（.ps1/.cmd），状态 在编；**与 skill 台账 S-004 同源，标互链 → S-004**（能力权威仍在各目录原生说明，不重复登记）。
  - 另探测 sifty（computer-cleanup 配套）与 ollama：PATH 及常见安装位置均**未探得**，如实标注「暂不登记、留待补」，未枚举 PATH 全部 exe。
- 添加日期统一记「巡检登记 2026-09-04（实际安装历史待补）」，与既有 skill 台账口径一致。

## 四、部署与核验状态
- 草案.html v0.11：候选 0 / 空收件箱，定案横幅 + 四条定案轨迹。✔
- 总则.html v0.7：已批准 4（Z-01~Z-04），含 id=z-04 卡与结论板原文，胶囊「全部 4 / 文件与存储规范 3」。✔
- 执行结构.html v0.8：正式展开 4，E-04 三小节齐全、索引表增行、返回锚点 总则.html#z-04。✔
- CLAUDE.md：末尾含「总则 Z-04 · MCP 服务与 CLI 工具台账」镜像（5 条）；Z-01 序文与 Z-02/Z-03 块一字未损（只 append）。✔
- D:\mcp\index.html 与 D:\cli\index.html：已建、内容据实（MCP 0 条 / CLI 2 条）。✔
- 双向锚点：总则 z-04 卡 [执行详解 ▸]→执行结构#E-04；执行结构 E-04 [◂ 返回总则]→总则#z-04。✔

## 五、留白 / 待办
- 本机**已运行中**的既有会话需重启后方读到新的 CLAUDE.md 镜像；新开会话即生效。
- MCP：当前 0 在编；日后添加 MCP 服务器时按 Z-04 字段补记 MCP-001…，走 claude mcp add / remove，勿手工改配置段。
- CLI：sifty / ollama 未探得，若后续定位（装毕/进 PATH）即补记；claude 与 tts_open_toolkit 的实际安装历史日期待补。
- npx/uvx 即拉即跑与工具自动托管类的缓存物理收拢，属系统级变更，另行批准后执行。
