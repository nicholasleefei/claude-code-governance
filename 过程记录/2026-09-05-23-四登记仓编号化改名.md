# 会议纪要 · 2026-09-05 · 四登记仓编号化改名

- 日期：2026-09-05
- 性质：用户要求给重要文件夹编号、使在文件列表中置顶；经三轮问答逐项拍板 → 物理改名四个登记仓 E:\model→E:\01-model、E:\skill→E:\02-skill、E:\mcp→E:\03-mcp、E:\cli→E:\04-cli ＋ 全机接线与引用同步 ＋ 小莫活档一并更新 ＋ 纪要 ＋ git
- 与会：用户（逐项拍板 · 定名 01-model 样式）、书记（本批落档）
- 说明：只改四个「登记仓」文件夹名；治理仓 E:\claude code总则 本身不改名；小莫 live 活档一起更新、其 过程记录 只增不改不动；ollama 由用户重启。

## 一、背景与拍板

- 动因：E 盘顶层平级文件夹多（治理仓 / 助理小莫 / ai-env / ComfyUI / 四个登记仓等），用户希望区分出四个「登记仓」文件夹、并在文件列表中排到顶部（示意「01model」）。
- 逐项拍板（三轮问答）：
  1. 范围＝只改四个登记仓 model/skill/mcp/cli（治理仓 E:\claude code总则 不改名）。
  2. 命名＝`01-model` 连字符样式 → model→01-model、skill→02-skill、mcp→03-mcp、cli→04-cli。
  3. 流程＝走治理流程：改权威稿与 CLAUDE.md 镜像、留纪要、git 密提交。
  4. E:\model 属实体仓（模型 M-001… + _cache，且连环境变量与 ComfyUI junction/yaml）→「全改＋全部接线同步」。
  5. Ollama 由用户自行重启（读取新 OLLAMA_MODELS）。
  6. 小莫 live 活档一起更新（过程记录除外）。

## 二、执行清单

- 物理改名：四仓顶层列表置顶为 01-model / 02-skill / 03-mcp / 04-cli。
- 系统接线：用户级环境变量 HF_HOME / OLLAMA_MODELS / TORCH_HOME / XDG_CACHE_HOME → E:\01-model\_cache\…；ComfyUI models 四个 junction（diffusion_models / loras / text_encoders / vae）重建指向 E:\01-model\M-001…；`extra_model_paths.yaml`、`run_h3.py` 注释、ai-env 三脚本（test_qwen3asr / test_qwen3tts / test_qwen3vl）路径同步。
- 引用同步（文档 · 镜像 · 台账 · 说明书 · Skill）：
  - 权威稿：总则.html（v0.14→v0.15，变更记录点名本纪要）、执行结构.html、会议章程.html、草案.html、claude code的总则安装提示词.txt——旧路径全部改写现名；
  - 用户级 CLAUDE.md（单向镜像）同步；
  - 四本台账 index.html（E:\01-model / E:\02-skill / E:\03-mcp / E:\04-cli）标题与全部路径；
  - E:\01-model 内 M-001~M-006 六份模型说明书 index.html；
  - 用户级 skills：external-link-inspection、multi-agent-collab 两 SKILL.md；
  - 小莫 live 活档：落地蓝图.html / 小莫专属Skill库.html / 交互守则.html / 画像卡.md / _稿件\小莫设计总纲.md。
- 明确不改（历史/运行时留档）：全部 过程记录（append-only）；小莫《知会给小希与总则·小莫出现.md》（留档通知）；ai-env 下 ollama_serve2/3.log（运行历史日志）；第三方示例引用（如 ComfyUI-GGUF README 的 E:\models\ 复数示例＝假阳性，跳过）。

## 三、残留核对（Task #4）

- 治理仓内旧路径仅存于：过程记录（只增不改，合理）+ 总则.html v0.15 变更记录的映射说明句（有意保留「旧→新」对照）。
- 用户级 skills / ~/.claude.json / settings.json：零残留。
- ComfyUI extra_model_paths.yaml / run_h3.py：零残留。
- ai-env：旧路径仅存于 ollama 运行历史日志。
- 环境变量：User 域＝新值；Machine 域空；Process 域为改名前的旧继承值（重启 ollama / 新开进程后生效）。

## 四、后续（用户执行）与遗留提示

- 由用户重启 ollama（及其启动载体，确保进程读到新 OLLAMA_MODELS=E:\01-model\_cache\ollama）后核验 `ollama list` 正常。
- 既有不一致（非本次四仓范围，另议）：部分小莫文档与 E:\02-skill 台账 S-011 行仍写工作区旧称 `E:\claude code助理多多`，实际文件夹已为 `E:\claude code助理小莫`。

## 五、落档与核验

- 改动文件清单（git 仓内）：总则.html（v0.14→v0.15）、执行结构.html、会议章程.html、草案.html、claude code的总则安装提示词.txt、本纪要。
- 明文凭据自查：通过——本批全程为路径与规则文本，无真实密钥；git 提交后工作区干净。
- git：`<hash 回填>`，已推送 github.com/nicholasleefei/claude-code-governance。
