# 会议纪要 · 2026-09-03 · 第二条总则 Z-02 定案与全机部署

## 一、会议决议
用户批准「模型文件统一仓 · D:\model」纳入总则（T-02 → Z-02）。

**总则 Z-02 · 模型文件统一仓（结论板原文）**
本机所有模型文件的默认落点为 D:\model\，一律不落入项目目录。D:\model\ 按「编号-模型名@版本」一模型一夹；根目录 index.html 总台账负责编号登记与查找，每夹内 index.html 为该模型说明书；任务用模型先查台账、有则复用、不重复下载。工具自带缓存经环境变量收拢至 D:\model\_cache\<工具>\（不拆一模型一夹）。项目内联调必需的小模型为例外，须登记台账并用 .gitignore 排除。定期巡检：凡 D:\model 之外的大体量模型文件一律归拢或登记。

## 二、定案要点（用户确认按主会话方案执行）
1. 编号：采用「总台账 + 编号文件夹」两层——D:\model\index.html 总台账（M-001…），夹名＝编号-模型名@版本；编号只增不重、删除不回收。
2. 工具缓存：经环境变量收拢到 D:\model\_cache\<工具>\（哈希散装，不拆一模型一夹）；属系统级变更，另行清单待批。
3. 巡检兜底写入条文（治"遗漏"）。
4. 例外：项目内联调必需的小模型可放 <项目>\models\，须登记台账 + .gitignore 排除。

## 三、P-8 并行书记团执行（第二次实战）
- 书记·草案 → 草案.html v0.7：T-02 清位，回到空收件箱并留定案痕迹。
- 书记·总则 → 总则.html v0.5：新增 Z-02 结论卡（id=z-02，分类「文件与存储规范」），Z-01/Z-02 双卡，计数 2；同一轮把 Z-02 条文镜像**追加**到 C:\Users\pc\.claude\CLAUDE.md（保留既有 Z-01 内容不动）。
- 书记·执行 → 执行结构.html v0.6：新增正式 E-02 展开（id=E-02：落地说明＋通俗解释＋名词解释，词条 18 项），索引表增行。
- 均已核验（锚点 总则.html#z-02 ↔ 执行结构.html#E-02 连通；全局 CLAUDE.md 尾部可见 Z-02 镜像）。

## 四、环境变量（系统级）待批 → 已执行
HF_HOME / OLLAMA_MODELS / XDG_CACHE_HOME / TORCH_HOME 等归拢清单尚未交给用户、也未改动系统——等主会话交付清单后逐项批准再动。
> **追记（2026-09-04）**：清单已交付，用户批准（4 项）。已用 setx 写入用户级变量并全部返回 SUCCESS：HF_HOME→D:\model\_cache\huggingface、OLLAMA_MODELS→D:\model\_cache\ollama、XDG_CACHE_HOME→D:\model\_cache\xdg、TORCH_HOME→D:\model\_cache\torch；D:\model 及四个 _cache 子目录已预建。新进程/新 shell 读取，既有运行中的会话/工具需重启后方可见。

## 五、过程备注
- 用户在书记团开工期间提问"为什么我发的文字要等"——主会话已解释：书记并行只解决"写文件不必等我"，主会话回话仍单线程；后续用户提议增设"书记长"助理以压缩主会话编排耗时（见 纪要 08）。
