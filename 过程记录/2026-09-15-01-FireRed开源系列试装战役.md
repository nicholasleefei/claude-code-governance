# 2026-09-15-01 · 小红书 FireRedTeam 开源系列试装战役（交接纪要）

> 类型：跨会话复杂工作交接纪要（Z-08）。状态截至 2026-09-15 深夜；只增不改，后续进展以文末「补记」追加。
> 主会话目录：`E:\claude code临时会话`；并行协作会话：tk-shop-visuals-34（生产侧研究，互不占卡）。

## 一、任务缘起与范围

飞总转来热文，列举小红书 FireRedTeam 开源多模态系列六件，原话指令：**"你看一下这些开源模型我们能不能装，你都装一下试试"**。机型基准：RTX 4090D 24GB / 64GB RAM / Windows 11，权重落 `E:\01-model`（Z-02）、工具实体落 `E:\tools`（Z-18）、台账登记（Z-02/Z-17）、凭据零明文（Z-09）。

| 文中名号 | 一手核实结论 | 编号 | 结果 |
|---|---|---|---|
| FireRed-Image-Edit 1.1 | 真实，指令图像编辑，Apache-2.0 | **M-013** | ✅ 冒烟+生产实证双通过 |
| FireRed-OCR | 真实，Qwen3-VL 7B 文档 OCR，Apache-2.0 | **M-015** | ✅ 冒烟通过（在用） |
| FireRed-ASR（FireRedASR-AED-L） | 真实，中英 ASR，Apache-2.0 | **M-016** | ✅ 冒烟通过（在用） |
| FireRedTTS3 | 真实，零样本克隆/语音编辑，Apache-2.0 + 克隆学术研究限定 | **M-017** | ✅ 冒烟+ASR 回环通过（在用） |
| FireRed-OpenStoryline | 真实，Agent 短视频框架，Apache-2.0 | TOOL-012（框架无权重编号） | 🔶 框架安装收尾中，key 飞总以后自配 |
| FireRed-Chat（文本 LLM） | **查无此仓/此模型**（FireRedTeam 无该发布，疑似网文凑名） | — | ❌ 不装不登 |

## 二、各件落地事实

### M-013 FireRed-Image-Edit 1.1（本战役最大坑）

- 权重（`E:\01-model\M-013-FireRed-Image-Edit\`）：unet Q4_K_M GGUF 13,065,746,976 字节 + 文本编码器 Q8 8,098,523,712 字节（**text-only，339 张量无视觉塔**）+ **mmproj 视觉塔 853,119,712 字节（关键伴侣件，来自 ModelScope `ggml-org/Qwen2.5-VL-7B-Instruct-GGUF`，非 FireRed 仓）** + VAE 253,806,246 + Lightning-8steps LoRA 849,542,776。
- **mmproj 陷阱**：官方仓自带 Q8 是纯文本塔，单挂则视觉塔报 `mat1 and mat2 shapes cannot be multiplied (784x1280 and 3840x1280)`，GGUF 节点日志 "Can't find mmproj file ... broken!"。必须同目录放 ggml-org 的 mmproj，city96 ComfyUI-GGUF 按文件名自动配对；**配对只在进程启动时扫描，运行中补文件必须重启 ComfyUI**。官方替代 15.8GB bf16 编码器曾试下，废弃删除（Q8+mmproj 8.4GB 更优）。
- 24GB 跑法：`--lowvram`，CLIP(Q8+mmproj 8.4GB 编码后卸载) → UNET Q4 12.2GB（8 步 euler/simple 热采样约 32s）→ VAE 0.24GB，分级换入不 OOM。参数：Lightning LoRA 1.0 + ModelSamplingAuraFlow shift 3.1 + CFGNorm 1.0 + KSampler steps8/cfg1.0/denoise1.0，TextEncodeQwenImageEditPlus。
- 冒烟：`E:\ai-env\test_m013_comfy.py`（14 节点 API 图打隔离实例 **127.0.0.1:8191**；上传必须显式 `Content-Type: multipart/form-data; boundary=`，否则 aiohttp 按 urlencoded 解析报 0x89），红圆改绿冷启动 145s、热采样约 32s。
- 生产侧实证（并行会话）：第 24 轮 f24_s1（seed 2401）整机一次重绘定板——只喂抽象材质 swatch（image2=c19_ref_panel、image3=c24_silver_swatch）即把哑光柱面改镜面铬，三镜/闪光灯/四向开孔/外屏黑圆/侧键全保，白底 253.7；真机热路径约 63s/张；服务技能 S-037 ecom-white-hero（research\material_lab 三脚本）。
- yaml：隔离用 `E:\ai-env\m013_extra_paths.yaml`；共享 `E:\ComfyUI\extra_model_paths.yaml` 已两会话交叉核对后追加 `m-013-firered-image-edit` 段（2026-09-15），**不重启 8188——重启门：等飞总在场、群里喊过再动**。

### M-015 FireRed-OCR

- 权重 model.safetensors 4,255,140,312 字节 bf16（Qwen3VLForConditionalGeneration），工具代码 E:\tools\FireRed-OCR（git 9a368c2）。
- 宿主 transformers（py-llm venv，bf16/device_map=auto/max_new_tokens 8192）。冒烟 6/6 关键词 PASS，表格→HTML、公式→LaTeX。Apache-2.0。

### M-016 FireRedASR-AED-L

- 权重 model.pth.tar 4,678,597,714 字节（checkpoint 内嵌 args；config.yaml 0 字节占位）+ cmvn/dict/BPE1000；工具代码 E:\tools\FireRedASR（git 834635e）。
- 冒烟 4 条官方样例 3 条逐字一致、会议长句开头仅一虚词差。默认 CPU（rtf≈4.39），批量 `--use_gpu 1`。
- 兼容点：torch≥2.6 须 import 前兜底 `torch.load(weights_only=False)`。Apache-2.0。

### M-017 FireRedTTS3

- 权重（`E:\01-model\M-017-FireRedTTS3\`，合计约 19.4GiB）：redae 3,775,160,672（24kHz codec）+ fireredtts3_base 8,482,608,484（ICL 克隆）+ fireredtts3_instruct 8,475,259,708（音色设计/语义/声学编辑）+ campplus 29,357,703 + Qwen 分词器。工具代码 E:\tools\FireRedTTS3（git 1d32ba7，经 gitclone.com 代理）。
- **Windows 适配（改官方源码 3 处，有注释，git pull 后复查重打）**：redae.py×2 + llm/fireredtts3_base.py 的 Qwen3_1_7B_ConfigDict×1，`flash_attention_2 → sdpa`（模型声明 _supports_sdpa）。fastText 关（use_fasttext=False）、LLM TN 关（use_llm_tn=False，**全程无 key**）、wetext 惰性导入本地规整（use_wetext=True）；faster-whisper/torchcodec 包内零引用。
- 专用 venv `E:\ai-env\py-fireredtts3`（Python 3.13.2）：**torch/torchaudio 2.11.0+cu128 由 py-llm 本地同盘 robocopy 移植**（官方钉 2.8；download.pytorch.org 国内零字节卡死、阿里 pytorch-wheels 稳态 ~105KB/s 不可用；2.11 所用 API——load/resample、autocast、inference_mode——均稳定面），transformers 5.6.2/soundfile 0.14 等走清华 PyPI。该 venv 曾因批量复制留下约 110 个缺 RECORD 的 dist-info 而报废，**删净重建**后正常。
- **torchaudio 2.11 音频 IO 坑（新）**：2.11 删除 soundfile/sox 后端，`torchaudio.load/save` 硬接 torchcodec（另需 FFmpeg），未装即 `ImportError: TorchCodec is required ...`，`backend=` 参数被忽略；**且失败时进程退出码仍报 0（重定向包装假象），不能只看退出码**。解法＝venv 级 `sitecustomize.py`（Python 启动自动导入）挂 `__import__` 钩子，**待 torchaudio 包初始化完成（`ta.__dict__` 里 load/save 均 callable；中途它先 import _torchcodec，提前打补丁会被随后的 def 覆盖）后**用 soundfile 替换 load/save。母本 `E:\ai-env\ta_soundfile_shim\sitecustomize.py`，已部署两个 venv；重建 venv 须重部署。functional.resample 不受影响。
- **冒烟结果（2026-09-15，✅）**：冷加载 67.3s（二次 OS 缓存热启 9.9s）；生成 17.7s 出 4.48s/24kHz 中文一句，peak 0.063（非静音、音量偏保守）；写 E:\ai-env\m017_tts3_smoke.wav。**M-016 ASR 闭环**（合成音经 resample 24k→16k 后转写）与目标文本"今天天气很好我们一起去公园散步吧"**15/15 字逐字一致**（脚本 test_m017_asr_check.py，CPU rtf≈1.0）。
- 许可红线：代码权重 Apache-2.0，但 README Usage Disclaimer 声明**零样本克隆能力仅供学术研究用途、禁非法使用**；商用克隆需授权/法务确认（台账已显著标注）。

### TOOL-012 FireRed-OpenStoryline（先装框架，收尾中）

- E:\tools\FireRed-OpenStoryline（git c9e9452，约 646M）：resource/ 426M 用 **Python zipfile** 解压成功 152 文件（PowerShell Expand-Archive 对 trailing-slash 目录条目会非法路径部分失败，勿用）；.storyline/models 有 transnetv2+MiniLM，skills×5。
- 入口 cli.py、agent_fastapi.py（web :8005）、MCP `PYTHONPATH=src python -m open_storyline.mcp.server`（streamable-http 127.0.0.1:8001/mcp）；transnet_device="cpu"。
- **config.toml 全部凭据字段保持空串**（llm/vlm/pexels/302/bytedance/minimax/dashscope），飞总本人以后填（Z-09）。无 key 框架冒烟标准＝依赖装齐 + import/load_settings/MCP 模块导入成功；build_agent 的 ChatOpenAI 构造不发网络请求。
- 专用 venv `E:\ai-env\py-storyline`（Python 3.13.2）：torch/torchaudio 2.11 同盘移植完成（物理 4 包齐，首次 import 报缺 typing_extensions 属正常，requirements 安装补齐）；shim 已部署；requirements 清华源安装**进行中**（pip_storyline.log）。

## 三、多会话协作（Z-05 实战）

- 双 ComfyUI 实例：8188 共享（生产侧主用）、8191 我隔离连调，权重同一份不复制；GPU 窗口用正式交接语"窗口给你"，窗口期零推理；M-017 跑完后 8191 `/free` 两次复核 **vram_free 22.4GiB/24.0GiB** 回基线并已通报，TTS 进程退出、GPU 全空。
- 共享配置改动两会话**先发全文核对、不直接改**；m-013 段对方统一风格、我落笔；定**重启门**（谁都不许单方重启 8188，等飞总在场群里喊）。
- 新踩坑双向共享：/free 连发两次（--lowvram 第一次只回 7.3GB）、multipart 缺 boundary 0x89、mmproj 启动扫描、torchaudio 2.11 torchcodec 坑与 sitecustomize 反身补丁。

## 四、台账更新（2026-09-15）

- `E:\01-model\index.html`：补登 M-013、M-015、M-016、M-017 四行；四夹各有 index.html 说明书。M-017 状态已回填实测（可用）。
- `E:\05-tool\index.html`：新增 TOOL-009～014（8→14）；v0.4 变更记录首登；**v0.5** 记 M-017/TOOL-011/013 转在用、torch 2.11 移植与 torchaudio shim；8191 判定为同实体不同启动参数不单列。

## 五、可复用经验沉淀

1. **llama.cpp 系 VLM 的 text-only Q8 + mmproj 分包**：ComfyUI-GGUF 同目录按名配对、启动时扫描；遇 `784x1280/3840x1280` 矩阵乘错即查 mmproj。
2. **国内装 torch cu 轮子**：download.pytorch.org 可能零字节卡死；先查本机其他 venv 同版本可同盘 robocopy 移植（torch wheel 可重定位，dist-info 一起逐目录复制），轻量依赖走清华 PyPI；偏离官方钉版要核对所用 API 是否稳定面并登记。
3. **robocopy**：整目录复制要逐目录 `robocopy src\d1 dst\d1 /E`（把目录名当过滤器会只建空夹）；退出码 1=成功复制。
4. venv 整盘复制 site-packages 会留下**缺 RECORD 的 dist-info**，pip 报 uninstall-no-record-file；损坏面大时删 venv 重建，不抢救。
5. **torchaudio 2.11 音频 IO**：无 torchcodec 即 load/save 全废；venv 级 sitecustomize + soundfile 钩子补丁（母本 E:\ai-env\ta_soundfile_shim），注意"包初始化完成后再打"的反身坑。后台重定向脚本不能只信退出码，要看日志关键行。
6. Windows 跑硬编码 flash_attention_2 的新模型：查 `_supports_sdpa`，改 sdpa；fasttext/wetext 多为惰性导入可关。
7. Windows 解压缩优先 Python zipfile，慎用 Expand-Archive。
8. WDDM 下 nvidia-smi 每进程显存 [N/A]，以 ComfyUI /system_stats 的 vram_free 为准；--lowvram 收尾 /free 连发两次。

## 六、待办交接

- [ ] OpenStoryline requirements 装完（日志 E:\ai-env\pip_storyline.log；py3.13 下 funasr/av 等 wheel 摩擦随遇随解）→ 无 key 框架冒烟（load_settings + import open_storyline 包/MCP 节点模块 + MCP 起监听）→ 更新 TOOL-012/014 状态与本纪要补记。
- [ ] 飞总本人配 OpenStoryline 各 API key（不催、不代填）；配好后端到端短视频流水线另案。
- [ ] 8188 重启门：飞总在场时群里喊一声再重启使 m-013 段生效。
- [ ] 本纪要按 Z-06 入治理 git 密提交；战役收尾后更新持久 memory + MEMORY.md 索引。
- [ ] 8191 实例：已回基线，进程暂留；长期不用可停（停前群里知会并行会话）。

---

## 补记一（2026-09-15 深夜）：OpenStoryline 框架装通，无 key 冒烟全绿

- py-storyline 依赖全部装成，**pip check 无冲突**。requirements 安装中实遇并解决三处本机适配：
  1. **transformers 必须钉 4.57.6**：requirements 不钉 transformers，pip 先选 5.10.4 再因 langchain-huggingface 1.2.0 的 `transformers<5`（full extra）一路回溯空转；钉 4.57.6（与 py-llm 同版）+ huggingface-hub<1 + tokenizers<1 后秒解。
  2. **langgraph-prebuilt 强制 1.0.8**：1.0.9 起 `prebuilt/tool_node.py` 引用 `langgraph.runtime.ExecutionInfo/ServerInfo`，该符号核心包 1.0.10 没有（到 1.1 才加入），而 langchain 1.2.4 钉 `langgraph<1.1.0,>=1.0.2`；langgraph-prebuilt 又漏声明对核心包的依赖，pip 错配成 prebuilt 1.0.13+core 1.0.10（且会拖 langchain-core 漂到 1.6.3）。经逐版本 wheel 解包定位分界在 1.0.9，定 **prebuilt 1.0.8 + core 1.2.7 + langgraph 1.0.10**，并删掉误装的孤儿 langchain-protocol。
  3. **editdistance 官方 C++ 在 MSVC 14.44/Python 3.13 编译失败**（_editdistance.cpp C2059，无 cp313 wheel；funasr 的离线 WER 依赖）。本地构建**纯 Python 垫片**（同名包 0.8.1，`eval(a,b)` O(n·m) DP，语义一致）：源码 `E:\ai-env\local_wheels\editdistance-py\`（pyproject+editdistance.py），pip 本地路径安装；重建 venv 需先装它再装 requirements。
- torchaudio shim 同样已部署进 py-storyline（sitecustomize.py）。
- **无 key 框架冒烟（E:\ai-env\test_openstoryline_framework.py）通过**：① load_settings 解析 config.toml，递归凭据扫描器（注意不能用裸 "token" 作特征，会误伤 max_tokens/tokens_per_clip 预算字段）断言全空；② create_server 扫描注册全部节点（funasr/moviepy/av/librosa/skillkit/langchain_huggingface 导入级全过）；③ **MCP 工具 20 个**（18 节点 + read_node_history + write_skills，另有 elementrec_text/elementrec_transition）。
- **真起服务验证**（E:\ai-env\probe_storyline_mcp.sh，仓根 PYTHONPATH=src，30 秒超时自动退）：streamable-http 监听 127.0.0.1:8001/mcp，initialize JSON-RPC 握手返回 `protocolVersion 2025-03-26 / serverInfo storyline 1.26.0`、下发 Mcp-Session-Id、HTTP 200。
- TOOL-012/014 状态改「框架就绪·待 key」，v0.6 变更记录已写。至此飞总"都装一下试试"六件中：四件模型可用、框架一件就绪待飞总填 key、一件（Chat）查无。剩：8188 重启门（等飞总在场）、配置 key 后端到端另案、Z-06 治理仓密提交。

**8191 终态（2026-09-16 凌晨）**：安装收尾时系统瞬时内存压力，后台 8191 隔离实例进程被 OS 结束（非手动）；复查端口无监听、进程全退，GPU 22.5GiB 空闲，8188 共享实例无影响（已群里知会并行会话）。8191 非持久服务，需要 M-013 隔离测试时按原启动参数（--lowvram --port 8191 + E:\ai-env\m013_extra_paths.yaml 等，见 M-013 说明书）重新拉起、先群里喊。纪要已于 b9bca8b 密提交并推送治理仓。
