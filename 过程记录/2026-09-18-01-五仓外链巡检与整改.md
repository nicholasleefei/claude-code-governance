# 2026-09-18-01 · Z-07 五仓外链巡检整改纪要（夜批）

> 类型：Z-07 外链巡检整改交接纪要（Z-08）。状态截至 2026-09-18 凌晨；只增不改，后续进展以文末「补记」追加。
> 执行：小希·书记长（执行班）率书记团甲/乙/丙/丁四名书记并行落文件，书记长亲执 lichtfeld MCP 修复启用（戊）与本纪要、git（己）。

## 一、缘起与范围

2026-09-17 晚约 20:5x，飞总授权整夹删除 `E:\my projects\bzchoice-ai-server`、`bzchoice-realtime-edge`，仅保留并落地 **bzchoice-link**（FastAPI 网关 + Cloudflare 隧道，本机）。只读书记团随后完成 Z-07 五仓外链巡检，各台账当时停在 2026-09-16 未同步；巡检结论经秘书长（= 顶层主会话）复核、飞总逐项拍板后，本批（2026-09-18 凌晨）落文件。

本批改件边界（七处文件 + 两处配置 + 本纪要）：

- 五仓台账：`E:\01-model\index.html`、`E:\02-skill\index.html`、`E:\03-mcp\index.html`、`E:\04-cli\index.html`（含新建跑通纪要）、`E:\05-tool\index.html`；
- `E:\my projects\index.html`；
- lichtfeld：`LichtFeld-Studio\.mcp.json`、`C:\Users\pc\.claude.json`（均先备份）；
- 本纪要。未装包、未改环境变量、未删迁任何其它文件、未读凭据内容（Z-09）。

## 二、飞总拍板（五项）

1. **M-018** faster-whisper-medium.en 杀净重下：主会话后台进程经本机代理（127.0.0.1:10808）直连 huggingface.co。
2. **llava、qwen3:0.6b** 保留并补登。
3. **volcengine 四技能**不逐条登，立「厂商 CLI 自动托管技能」统一口径成组登记。
4. **lichtfeld MCP** 修复并启用。
5. 其余台账同步项照办。

## 三、两处勘误（对巡检初报的更正）

1. **证书续签任务存在**：计划任务 `BZ-Link-CertRenew`（每月 1 号 CalendarTrigger，03:07，动作 `E:\my projects\bzchoice-link\scripts\cert_renew.cmd`，State=Ready，下次 2026-10-01 03:07；尚未首跑——09-16 首签走的是 cert_issue 一次性脚本，符合设计）。edge.bzchoice.cc 证书 2026-09-16 21:21 首签、有效期至 2026-12-15，月续签前置生效，**无断签风险**。旧拟名 BZ-Edge-CertRenew/BZ-Edge-Gateway 查无此任务（随合并改名 BZ-Link-*）。
2. **bzchoice-api 是第 8 个项目夹**：2026-09-17 23:55 新建，FastAPI 骨架；计划任务 `BZ-API-Server`（LogonTrigger，State=Ready）指向 `scripts\autostart_api.vbs`。

## 四、五仓核查与整改总览

| 仓 | 关键变更 | 结果 |
|---|---|---|
| 01-model | 新登 M-018（权重到位）、M-019 llava、M-020 qwen3、M-021 birefnet；M-002/M-007/M-008/M-009 历史注记维持 | ✅ |
| 02-skill | S-039/040/041 停用（随夹灭失待重建）；S-012/013/014 归档件灭失；S-036/038 联接注销、真身完好；新登 S-042 火山组；立两口径；41→42 条 | ✅ |
| 03-mcp | meta「用户级在编 0／项目级 1」；新登 MCP-001 lichtfeld（已修复启用）；0→1 条 | ✅ |
| 04-cli | CLI-001→2.1.274；CLI-004 迁址+任务链+互链；CLI-005 退场保留；CLI-006 独立化；新登 CLI-008 ollama；立基础运行时口径；首份跑通纪要建档并入索引；7→8 条 | ✅ |
| 05-tool | TOOL-015 改「在用·证书有效」+ 续签任务 + 凭据存在性备注；TOOL-002 CMake 原始 zip 未留存；v0.8 变更记录 | ✅ |
| 附：项目台账 | `E:\my projects\index.html` 总览 7→8，补登 #8 bzchoice-api；#3 bzchoice-link 加巡检在役注；生成日期 2026-09-18 | ✅（含一项待拍板，见七.3） |

六本 HTML 均经书记自查：标签配平（解析器/计数核对）、UTF-8 无乱码、零外部资源、离线可开。

## 五、整改逐条结果

### 5.1 01-model（甲书记）

- **M-018 faster-whisper-medium.en**：落条目前 `Test-Path` 由 False（下载中）转为 **True——本批执行期间主会话重下完成**，model.bin 实测 **1,527,904,330 字节，与目标逐字节一致**，config.json/tokenizer.json/vocabulary.txt/README.md 齐备（约 1.46 GiB 全量）。按拍板口径「在则按在役写」登为在役·权重到位；转写冒烟留待首次接入时补验，未杜撰冒烟记录。下载目录全程只做只读观测。
- **M-019 llava:latest**（ID 8dd30f6b0cb1，4.7 GB）、**M-020 qwen3:0.6b**（ID 7df6b6e09427，522 MB）：仿 M-004 口径登记，实体随用户级环境变量 `OLLAMA_MODELS=E:\01-model\_cache\ollama` 收拢（`C:\Users\pc\.ollama` 仅密钥对）；gpt-oss:20b（17052f91a42e，13 GB）已登 M-004 不重复；当日 `ollama list` 三件在册实测。
- **M-021 birefnet-general.onnx**：路径勘正——给定路径 `_cache\rembg\birefnet-general.onnx` 实测 False，真实实体为 `E:\01-model\_cache\rembg\models\birefnet-general\birefnet-general.onnx`（rembg 标准 `models\<名>\` 布局），实测 **972,666,916 字节（≈927.6 MiB）**；缓存权重实体，宿主 rembg（rembg-env），服务电商抠图链路；与 S-036 ecom-cutout-rectify 互链（其台账明载 BiRefNet 抠主体、权重走 01-model 缓存），S-037/038 消费其 rgba 成品；许可 MIT 可商用（沿 S-036 口径）。
- M-002 .incomplete、M-007/M-008 停用留号、M-009 夹名遗留自披，原样维持未动文件。

### 5.2 02-skill（乙书记）

- **S-039 BZ-white-hero / S-040 BZ-ecom-cutout / S-041 BZ-ecom-cutout-only**：徽章改「停用（随夹灭失）」，各加 2026-09-18 注记——2026-09-17 经飞总授权整夹删除，各自 yaml 规则、配套经验本、工作区「技能空间\…」及 S-036/038 目录联接与指纹授权（authorized_skills.json）同批灭失，待新 BZ-API 项目重建。（「用户授权」沿台账既定称谓落「飞总授权」。）
- **S-012/013/014**：徽章保留退役历史，加注「2026-09-14 归档件已随旧夹 2026-09-17 灭失，编号退役不复用」。
- **S-036/038**：加注 bzchoice 侧目录联接与指纹授权 2026-09-17 注销，仅 basic-tool-skills 研究仓真身可用（只读核实两真身夹在），当前无上网通道、真身完好。
- **S-042（新）volcengine 自动托管技能组**：volcengine-cli / -find-skills / -knowledge-search / -troubleshooting 四技能成组一条；双位 8 夹 Test-Path 全在（`C:\Users\pc\.claude\skills\` 与 `C:\Users\pc\.agents\skills\`），mtime 区间 2026-09-06 09:20:20–09:52:53，最早者作安装日期；`npm ls -g` 核 @volcengine/cli@1.1.5；能力以原生 SKILL.md 为准、台账只索引。
- 立两口径：①厂商 CLI 自动托管技能不逐条登、成组一条（首例 S-042）；②Claude Code 内置技能（随 claude.exe 分发、无磁盘实体）与插件市场缓存（未安装未启用）不算注册实体。

### 5.3 03-mcp / 04-cli（丙书记）

- 03-mcp：meta「已登记 1／用户级在编 0／项目级 1」；MCP-001 lichtfeld 九字段写全，状态定稿见第六节。
- 04-cli：meta 8 条（在编 7／退场保留 1），巡检日期 2026-09-18。
  - CLI-001 claude 2.1.259→**2.1.274**（实测 `2.1.274 (Claude Code)`）。
  - CLI-004 cloudflared：实体 `E:\my projects\bzchoice-link\tools\cloudflared.exe`，实测 `cloudflared version 2026.9.0 (built 2026-09-09T08:55 UTC)`；启动链＝计划任务 BZ-Link-Tunnel（State=Ready）→ `scripts\autostart_tunnel.vbs`（WSH 隐藏窗）→ `autostart_tunnel.cmd`（延时 6 秒、写 logs\tunnel.log）→ `cloudflared.exe --no-autoupdate tunnel run bzchoice`；进程实测在跑；ingress：api.bzchoice.cc→127.0.0.1:8000、realtime.bzchoice.cc→127.0.0.1:8443；凭据只记位置（项目外 `C:\Users\pc\.cloudflared\`）不读内容。
  - CLI-005 yt-dlp：2026-09-17 随 bzchoice-ai-server 退场（授权删除、未迁），编号保留不复用，旧路径删除线，历史用法/踩坑全保留。
  - CLI-006 ffmpeg：删尽「yt-dlp 配套」表述，改独立音视频处理底座；实体 `E:\tools\ffmpeg\ffmpeg-9.0.1-essentials_build\bin\` 未动。
  - **CLI-008 ollama v0.34.0**（CLI-007 已被 ziniao-cli 占用，顺延不复用）：`C:\Users\pc\AppData\Local\Programs\Ollama\ollama.exe`（PATH，官方安装版默认位，按 Z-18 不迁 E:\tools），互链 M-004/M-019/M-020。
  - 立口径：基础运行时 node/npm/git/python/pip/cargo/code 等不逐条登记。sifty 复查仍不在 PATH，维持待查。
- 新档 `E:\04-cli\跑通纪要\CLI-004-cloudflared.html`（该目录首份纪要，照 index.html 版式）：实体/版本实测原文/四步任务链/两坑（全局旗标位置、vbs/cmd 须纯 ASCII+延时）/502 vs 404 判读/凭据只记位置/五条实测命令留痕/2026-09-18 书记团命令行实测；并挂入同目录 `index.html` 索引表（原表「暂无记录」），台账↔纪要三向互链。

### 5.4 05-tool 与项目台账（丁书记）

- **TOOL-015**：绿色「在用·证书有效」。lego 四件套（`C:\Users\pc\.bzedge\lego\certificates`，均 2026-09-16 21:21:24）：edge.bzchoice.cc.crt 4,820 B／.issuer.crt 3,526 B／.json 315 B／.key 241 B；BZ-Link-CertRenew 运行中、无断签风险；引用项目改 bzchoice-link（#3）与 bzchoice-api（#8），两旧夹删除留痕。凭据存在性备注（Z-09，仅文件名/大小/时间，**内容未读**）：`C:\Users\pc\.bzedge\cf_token.txt`（53 B）、`ws_token.txt`（32 B），bzchoice-link 项目目录内无凭据文件。
- **TOOL-002 CMake**：原始 zip 未留存（`E:\tools\_downloads` 现存仅 colmap-x64-windows-cuda.zip 363.3 MB／ffmpeg-release-essentials.zip 106.1 MB／lego_v5.4.1_windows_amd64.zip 20.5 MB）。
- **项目台账 #8 bzchoice-api**：实测 27,819,182 B（26.5 MiB）／1,634 文件，.venv 占 99.9%（27,787,619 B／1,617 文件；去依赖仅 17 文件约 30.8 KiB）；requirements 钉 fastapi==0.141.1、uvicorn==0.53.0；selftest 6/6、已公网验证 200；绿色「正常项目（v0.1 骨架·业务接口待加）」。总览 7→8、生成日期 2026-09-18、署名加「09-18 补登 bzchoice-api」；#3 bzchoice-link 加巡检在役 bullet（BZ-Link-Gateway/Tunnel/CertRenew 均 Ready）。两旧夹在旧台账中本无独立行，删除事实在 #3/#8 带日期留痕。

## 六、戊 · lichtfeld MCP 修复启用全程（书记长亲执）

1. **故障定性**：`.mcp.json`（server 名 `lichtfeld`，stdio）`command` 原为 `python3`，在 Windows 解析到 `%LOCALAPPDATA%\Microsoft\WindowsApps\python3.exe`——0 字节微软商店别名桩；从未批准启用。bridge 脚本 `scripts\lichtfeld_mcp_bridge.py` 经通读仅用标准库（json/os/subprocess/urllib/pathlib 等，fcntl 有 Windows 回退），无第三方依赖；initialize/ping 由 bridge 本地应答，按需才拉起 LichtFeld Studio 本体（HTTP 127.0.0.1:45677/mcp）。
2. **备份**：`.mcp.json` → 同目录 `.mcp.json.bak-20260918`；`C:\Users\pc\.claude.json` → `.claude.json.bak-20260918`（83,790 B）。
3. **解释器选定**：项目内无 venv；E:\ai-env 四个 venv 不采用；定系统 **Python 3.13.2 `C:\Users\pc\AppData\Local\Programs\Python\Python313\python.exe`**（PATH 在编、不随任一项目 venv 存亡）。
4. **改配置**：command 改该绝对路径，args 保持 `["scripts/lichtfeld_mcp_bridge.py"]`。
5. **冒烟**：以该解释器启动 bridge 喂 JSON-RPC initialize（5 秒超时），退出码 0，返回 `serverInfo={name: lichtfeld-mcp-bridge, version: 1.0.0}`、protocolVersion 2024-11-05，无 ImportError/Traceback，stdin EOF 干净退出。（途中 PowerShell StandardInput 疑似写入 BOM 致首行 header 被污染，改用 Python 驱动复测通过——真实 MCP 客户端行为即无 BOM UTF-8。）
6. **启用**：用 Python（遵令不用 PS 5.1 ConvertFrom-Json）读 `.claude.json`；projects 段原无该子项目键（父夹 `E:/my projects/3D Gaussian Splatting` 有键），按既有前斜杠规范形新建 `E:/my projects/3D Gaussian Splatting/LichtFeld-Studio`，`enabledMcpjsonServers=["lichtfeld"]`，临时文件 + `os.replace` 原子写（83,790→83,921 B），复验两个 JSON 均可解析。
7. MCP-001 台账定稿：**已修复并启用（2026-09-18）**，含上述解释器/冒烟/原子写/双备份事实，并载明限制（见七.1）。

## 七、未竟项与留给飞总的动作

1. **lichtfeld 首次会话确认**：需飞总在 `E:\my projects\3D Gaussian Splatting\LichtFeld-Studio` 目录开一次 Claude Code 会话，过首次信任/交互确认（预置键不含 hasTrustDialogAccepted，不替飞总点信任）。另：LichtFeld Studio **本体 exe 尚未构建**（无 build/dist 目录），initialize 与缓存 tools/list 可用；首次缓存未命中的 tools/list 会尝试拉起本体，构建前实时工具不可用——要用实时工具须先构建本体。
2. **M-018**：权重已完整到位，但转写冒烟待首次接入时补验；下载残留 0 字节 .incomplete/lock 文件由主会话稍后清理（与台账书记无关）。
3. **第 9 个文件夹待拍板**：`E:\my projects` 物理上现有 9 个一层文件夹，多出「AI‑generated short videos」（连字符为 U+2011 非 ASCII，目录时间 2026-09-18 00:28–00:34），全夹仅 1 个 0 字节 txt 草稿，无代码无 README。丁书记未越权登记，已在项目台账「待处置」加卡⑤如实记录；请飞总拍板：登记 / 改名 / 并夹。
4. sifty CLI 仍不在 PATH，04-cli 台账维持待查。
5. S-039/040/041 待新 BZ-API 项目重建后重新登记（编号不复活）。

## 八、纪律与安全自查

- Z-09：本纪要与全部台账零明文凭据——cf_token/ws_token 仅记文件名与大小、隧道凭据仅记目录位置；未对任何凭据文件 Get-Content。
- 未装包、未改环境变量、未改计划任务；M-018 目录全程只读观测；备份件（.mcp.json.bak-20260918、.claude.json.bak-20260918）保留备查。
- git：仅 `git add` 本纪要一篇；未跟踪的 `过程记录\2026-09-13-06-…\立项.md` 与本批无关，不带入提交。
