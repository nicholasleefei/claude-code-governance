# 2026-09-18-02 · 打不开软件残渣 / 注册表深清 / C 盘清理 / WPS·飞书重装纪要

> 类型：本机维护交接纪要（Z-08）。状态截至 2026-09-18 晚；只增不改，后续以文末「补记」追加。
> 执行：秘书长（= 顶层主会话）全程会话内执行（用户多次拍板授权），未派书记团。
> 上一篇：2026-09-18-01 五仓外链巡检整改纪要。

## 一、背景与根因

飞总：「把电脑中打不开的软件相关的文件夹也删除」。体检结论：**D 盘程序目录已被整体清空**（现仅余 `D5 WorkSpace`、`xwechat_files`、pagefile），但 65+ 失效快捷方式、ARP 注册表项、C 盘残留文件夹、COM/Shell 扩展注册全部留存，表现为右键空项、点快捷方式报错。随后追加「清理 C 盘」「清注册表空项」「WPS/飞书下载重装」。

## 二、授权拍板节点

1. 清理范围：A 轻量残渣 + B Adobe 全家 + C Autodesk 全家 + D Red Giant（全选）。
2. VS Code 用户配置/扩展（Roaming\Code + .vscode，2.9 GB）保留。
3. WPS、飞书：保留数据，下载官方包重装。
4. 回收站清空（不可逆）已授权；codex-runtimes 缓存 1.13 GB 可永久删（已删）。
5. WPSDrive 7.7GB + WPS Cloud Files 5.2GB（财务数据）不删，重装后教用户做仅在线/缓存外移优化（**尚未执行，待用户操作指导**）。

## 三、文件系统清理（文件夹均先进回收站，注册表先 .reg 备份）

- 死快捷方式：67 个（桌面/公共桌面/两处开始菜单），最终复扫 **0 个**；连带清空开始菜单空目录。
- 文件夹回收（约 13.6 GB 名义量）：
  - Adobe 9 处（Program Files/Common Files×2/ProgramData/AppData×4/Common Files x86）；
  - Autodesk 7 处（Program Files 3.5GB、Common Files×2、ProgramData 1.1GB、AppData×2、x86）；
  - ArcGIS/ESRI、SketchUp、Bentley、Pangolin、DasViewer、Red Giant（Magic Bullet 官方卸载+Bylodd msiexec 卸载）、QQ音乐缓存、应用宝 Androws、旧版 QQ(x86)、电脑管家、鲁大师等。
- 占用处理：删除孤立服务（AGMService、Autodesk Content Service、AdskLicensingService 等，逐键导出 .reg）、Adobe/Autodesk 计划任务（AdobeGCInvoker、CCXProcess 等）、Run 启动值（Adobe 更新器、Autodesk Sync/Access/Genuine、QQMusic、Acrobat Assistant 等）。
- 长路径 + SYSTEM owner 钉子文件（AcroPDF ActiveX/CoreSync/PDF joboptions 共 34 个 14.5MB）：经多轮提权后以 takeown+icacls 夺取所有权，移至桌面 `Adobe-leftover-delete-me` 交用户手删——**用户当晚已手动删除**。
- ARP 卸载信息：约 80 条死条目清除（规则：InstallLocation 在 D:、不存在或为空且厂商匹配；在役件全保留）。
- 未动：微信/腾讯会议/QQNT/紫鸟/Mars2022/D5/Rhino8/C4D/影刀/湘源控规8.0（1.9GB 在役）/全部银行控件；WPS/飞书/kingsoft 全部数据；VS Code 配置。

## 四、注册表深清（本轮新增，右键空项根因）

方法：reg.exe 原生导出三个 Classes\CLSID（64位9.9MB/32位26.7MB/CU）→ 解析 InprocServer32/LocalServer32 → 目标文件不存在且命中死厂商路径（Adobe/Autodesk/Bentley/ESRI/SketchUp/天正Tch10/Red Giant/Pangolin/D:\Users/D:\Program Files），在役厂商（Kingsoft/wps/Feishu/微信/QQ/会议/紫鸟/D5/Rhino 等）白名单排除。

| 类别 | 删除量 |
|---|---|
| ProgID（含 ESRI.ArcGIS.* 约 21,102 个、Adobe/Autodesk/天正等 500+） | 21,646 键（0 失败） |
| 死 CLSID（64/32/CU 合计） | 493 键次 |
| ContextMenuHandlers/静态动词/App Paths | 5 + 3 + 19 键 |
| Approved Shell 扩展值 | 8 |
| PreviewHandlers（Adobe PDF/Illustrator/dgpdf，64+32） | 6 |
| 厂商 SOFTWARE 树（Adobe/Autodesk/ESRI/SketchUp/Tangent/Bentley，HKLM×2视图+HKCU） | 15 树 |

工具教训：①. NET RegistryKey 从根键 DeleteSubKeyTree 深路径本次出现"无异常但未生效"，对残留键改用 `reg delete /f` 提权强删成功（26 删、16 已不存在）；②PS 注册表提供程序遇 `Classes\*` 字面键必须 `-LiteralPath`，否则被当通配符全枚举（两次长超时根因）；③删前全量备份 Classes 三 hive（共约 208MB .reg）。

## 五、C 盘清理（computer-cleanup skill 流程）

- sifty 用户级垃圾：1.9 GB 进回收站（临时/缩略图/浏览器缓存/OneDrive 日志/崩溃转储）；管理员级 Windows 临时/更新缓存 39 MB（余为占用中跳过）。
- codex-runtimes 缓存 1.13 GB 永久删除（可重建）。
- 回收站清空（Adobe/Autodesk 死文件夹+垃圾，约 4GB 实测口径）。
- **C 可用：357.4 → 374.7 GiB**（期间新装飞书/WPS 占约 2.4GB 后净值）。
- 未做：DISM 组件清理（盘不紧张）；WPS 云数据（财务，待优化非删除）。

## 六、WPS 与飞书重装（官方包，落 E:\tools\_installers 留档）

- 飞书 8.0.3 x64（feishucdn 官方直链，451,439,504 B 校验一致）→ 装至 `%LOCALAPPDATA%\Feishu`，已运行，Roaming\LarkShell 1.64GB 旧数据原样保留待登录关联。
- WPS：官网实时 JS 解析得官方当前包 WPS_Setup_17133（12.1.0.17133，242,996,520 B 一致）→ 安装后自动升级回 **12.1.0.28505**（即用户原版本），wps.exe 已运行；旧版夹 `…\WPS Office\12.1.0.17133`（1.04GB）保留作回滚，日后可清。
- 新快捷方式实测有效：WPS→LocalAppData ksolaunch.exe、飞书→LocalAppData Feishu.exe。
- 待办：WPS 云文档本地缓存（WPSDrive/WPS Cloud Files 共约 13GB）按用户选择指导优化（仅在线或移 D 盘），**未执行**。

## 七、备份位置（回滚凭据）

桌面 `cleanup-backup-2026-09-18\`：ARP/Run 全量 .reg（6 份）、逐服务 .reg、计划任务 XML、reg-scan\ 下 BACKUP-Classes64/32/CU.reg（全 Classes 备份）+ 各厂商树 .reg + 删除清单 7 个 XML、transcript1–10 全日志、elevated 脚本 7 个。验证稳妥后可整体归档/删除该夹。

## 八、未竟与注意

1. **湘源控规 8.0**（在役，1.9GB）依赖 AutoCAD 平台，AutoCAD 已随 D 盘灭失；要用须先重装 AutoCAD（2014/2023 均可，天正 T20 同理已无）。
2. TypeLib/Interface 下仍有大量死 COM 引用（ArcGIS 系），无 UI 与运行影响，未清（量上万、纯冗余）；如强迫症发作可后续按同法白名单清。
3. D 盘程序整体灭失的原因本次未追查；若非常态（如盘曾被误格式化/迁移），建议用户留意。
4. 全部纪要/台账/日志零明文凭据（Z-09 自查）：未接触任何凭据内容。
5. 05-tool 台账：本批删除软件均未在台账登记（台账在册的是 BuildTools/CMake/ffmpeg/Ollama/vcpkg/FireRed 等，均在役未动），无需改台账；两个新装包属普通应用软件不登记。
