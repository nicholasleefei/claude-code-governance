# 会议纪要 · 2026-09-03 · 第一条总则 Z-01 定案与全机部署

## 一、会议决议
用户批准将「适用范围」纳入总则，并按主会话提出的四道边界定案（用户：能接受，就按你的理解）。

**总则 Z-01 · 适用范围（结论板原文）**
本总则适用于本机（账号 pc）上启动的全部 Claude Code 会话——任意目录与项目，含本工作区自身。效力以「本机用户级配置可被读取」为界：读取该配置的形态（CLI／桌面端会话）在适用内；读取不到的形态（claude.ai 网页端、其他电脑与其他账号、企业托管层）不在其内。本总则为全机默认基线，效力自高而低：当次会话临时指令 ＞ 红线条款（不豁免）＞ 项目级规则（可覆盖软规则）＞ 总则软规则。

**定案的四道边界**
1. 机器/账号：本机 + 账号 pc；换机换号需各自部署。
2. 会话形态：效力边界＝用户级配置可被读取的地方；网页端（云端、读不到本机配置）不在内，如实承认够不着。
3. 效力层级：临时指令 ＞ 红线（系统 deny，不豁免）＞ 项目级（可覆盖软规则）＞ 总则软规则（基线）。
4. 覆盖范围：含本工作区自身；但「怎么开会」归章程管，不入总则。

## 二、首次实战 P-8 并行书记团（本次决议落地）
三个书记子 Agent 并行完成、互不覆盖，主会话未串行等待：
- 书记·草案 → 草案.html v0.5：T-01 清位，回到空收件箱并留定案痕迹。
- 书记·总则 → 总则.html v0.4：写入 Z-01 结论卡（id=z-01，分类「效力与总纲」，计数/胶囊/hero 更新），移除界面示意演示区；**同一轮**创建全局部署文件 C:\Users\pc\.claude\CLAUDE.md（全机镜像，软规则通道）。
- 书记·执行 → 执行结构.html v0.5：移除演示 e-demo1~3，写入正式 E-01 展开（id=E-01：落地说明＋通俗解释＋名词解释），更新索引表。
- 主会话 → 章程索引追加 -06；落本纪要。

## 三、部署与校验状态
- 全局文件 C:\Users\pc\.claude\CLAUDE.md 已创建（原本不存在，无覆盖）。内容＝Z-01 效力镜像＋优先级＋权威稿指向。
- 双向锚点已就位：总则.html#z-01 ↔ 执行结构.html#E-01。
- 建议用户侧校验：任取一个无关文件夹新开会话，/context 查看是否加载到全局 CLAUDE.md；/status 查看设置来源。
- 后续每批准新总则，均按此通道：软规则→追加镜像；红线→settings.json 权限（系统层），仍先经用户同意。

## 四、留白
Z-01 之外尚未有行为型内容条款（语言、红线、交付等）——等待会议逐条提出。
